YOUTUBE = "lingua_loop.integrations.youtube"

# @pytest.fixture(scope="session", autouse=True)
# def mock_ytt(mocker: MockerFixture):
#     mock_fetch_transcript = mocker.patch(
#         f"{YOUTUBE}.fetch_transcript"
#     )
#
#     mock_list_transcripts = mocker.patch(
#         f"{YOUTUBE}.list_transcripts"
#     )
#
#     mock_vido_has_transcript_in_language = mocker.patch(
#         f"{YOUTUBE}.video_has_transcript_in_language"
#     )
